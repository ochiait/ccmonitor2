<nav class="navbar navbar-default ">
  <div class="container-fluid">
  <!-- ヘッダー部分 ================ -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">料理教室予約</a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#nav_target">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
  <!-- 中央のナビゲーション部分 ================ -->
    <div class="collapse navbar-collapse" id="nav_target">
      <ul class="nav navbar-nav">
        <!-- リンクのみ -->
          <li class="active"><a href="#">レッスン</a></li>
          <li><a href="/pages/tbd1">Q＆A</a></li>
          <li><a href="/pages/tbd1">ブログ</a></li>
          <li><a href="/pages/tbd1">問い合わせ</a></li>
      </ul>
  <!-- 右寄せになる部分 ================ -->
      <ul class="nav navbar-nav navbar-right">
        <!-- リンクのみ -->
          <li><a href="/login/new">先生はこちら</a></li>
      </ul>
    </div>
  </div>
</nav>
 <!-- ナビ部分終わり ================ -->
<div class="container" style="padding:20px 0">
  <h1>レッスン詳細</h1>
     <table class="table table-striped table-bordered table-hover table-condensed">
        <thead>
          <tr><th>レッスンID</th><th>日付</th><th>開始時刻</th><th>終了時刻</th><th>レッスン名</th><th>料金</th><th>詳細</th></tr>
        </thead>
        <tbody>
          <tr>
            <td><%= @lesson.id %></td>
            <td><%= @lesson.date %></td>
            <td><%= @lesson.start_time %></td>
            <td><%= @lesson.end_time %></td>
            <td><%= @lesson.name %></td>
            <td><%= @lesson.price %></td>
            <td><%= @lesson.comment %></td>
         </tr>
        </tbody>
      </table>
<div class="container" style="padding:20px 0">
  <%= form_for @participant do |f| %>
    <div class="form-group">
      <h1>新規レッスン予約</h1>
      <%= f.hidden_field :lesson_id %>
      
      <%= label_tag :name, '名前', :class => 'control-label'  %>
      <%= f.text_field :name, :class => 'form-control', :placeholder => 'name' %>
  
      <%= label_tag :email, 'メールアドレス', :class => 'control-label'  %>
      <%= f.text_field :email, :class => 'form-control', :placeholder => 'email' %>
  
      <%= label_tag :tel, '電話番号', :class => 'control-label'  %>
      <%= f.text_field :tel, :class => 'form-control', :placeholder => 'TEL' %>
    </div>
  <div class="form-group">
     <%= f.submit "送信" , :class => 'btn btn-primary' %>
  </div>
 <% end %>
</div> 